# =================================================
#  Configuration for Project Sentinel
# =================================================
# Copy this file to .env and modify if necessary.
# The default values are optimized for the Docker setup.

# --- SERVER SETTINGS (for server.py) ---
MODEL_NAME=llama3
SQL_DB_PATH=db_data/company.db
DB_PATH=chroma_db
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# --- INGESTION SETTINGS (for ingest.py) ---
DATA_PATH=Data
EMBEDDING_MODEL_NAME=all-MiniLM-L6-v2
CHUNK_SIZE=1000
CHUNK_OVERLAP=200

# --- CLIENT SETTINGS (for client/src/App.jsx) ---
# IMPORTANT: When running with Docker, the frontend container must use the
# backend's service name ('backend') to communicate. Do not use 'localhost'.
VITE_API_URL=http://backend:8000

# --- OLLAMA SETTINGS ---
OLLAMA_HOST=http://host.docker.internal:11434
