# =================================================
#  Configuration for Project Sentinel
# =================================================
# Copy this file to .env and modify for your environment.

# --- DEPLOYMENT MODE ---
# Choose one of the following modes for the SQL database connection.

# --- Mode 1: On-Premise / Local SQLite File (Default) ---
# The path to the local SQLite database file.
# The backend will create this file if it doesn't exist.
SQL_DB_PATH=db_data/company.db
# Leave SQL_CONNECTION_STRING blank when using this mode.
SQL_CONNECTION_STRING=

# --- Mode 2: Cloud / Network Database ---
# A full SQLAlchemy connection string for connecting to a cloud or network database.
# Example for PostgreSQL: postgresql://user:password@host:port/database
# If you fill this in, it will be used instead of SQL_DB_PATH.
# SQL_CONNECTION_STRING=

# --- VECTOR DATABASE (for PDFs) ---
DB_PATH=chroma_db

# --- INGESTION SETTINGS (for ingest.py) ---
DATA_PATH=Data
EMBEDDING_MODEL_NAME=all-MiniLM-L6-v2
CHUNK_SIZE=1000
CHUNK_OVERLAP=200

# --- OLLAMA SETTINGS ---
# The address of the Ollama server.
# 'host.docker.internal' is a special DNS name that lets the Docker container
# connect to the host machine.
OLLAMA_HOST=http://host.docker.internal:11434
MODEL_NAME=llama3

# --- CLIENT SETTINGS (for client/src/App.jsx) ---
# IMPORTANT: When running with Docker, this must be the backend's service name.
VITE_API_URL=http://backend:8000
